# Quality Analysis Tools Requirements
# These are minimal dependencies for the quality tools to work

# Core Python packages (usually already available)
pathlib2>=2.3.0; python_version < '3.4'

# Optional but recommended for enhanced analysis
pylint>=2.15.0
flake8>=4.0.0
bandit>=1.7.0
safety>=2.0.0

# For generating reports and visualizations
matplotlib>=3.5.0
seaborn>=0.11.0

# For database operations (SQLite is built-in)
# No additional requirements needed for SQLite

# For Git integration (optional)
GitPython>=3.1.0
