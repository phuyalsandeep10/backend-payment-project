# ==========================================
# PRS SYSTEM CONFIGURATION
# ==========================================

# Django Core Settings
SECRET_KEY=your_secret_key_here_please_change_me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database Settings (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=payment_receiving_system
DB_USER=payment_receiving_system_user
DB_PASSWORD=2FMBobIxcHx39jkNzrlVrHyIIWoL1GhZ
DB_HOST=dpg-d1r2t8ur433s739pv13g-a
DB_PORT=5432
# Super Admin Credentials (for Django admin & OTP)
ADMIN_USER=superadmin
ADMIN_EMAIL=shishirkafle44@gmail.com
ADMIN_PASS=password 

# ==========================================
# EMAIL CONFIGURATION - GMAIL SMTP
# ==========================================

# CRITICAL: Enable real email sending (not console)
USE_SMTP_EMAIL=True

# Gmail SMTP Server Settings
EMAIL_HOST=smtp.gmail.com          # Gmail's SMTP server
EMAIL_PORT=587                     # Gmail SMTP port (with TLS)
EMAIL_USE_TLS=True                 # Enable TLS encryption

# Gmail Account Credentials
EMAIL_HOST_USER=     # Your Gmail address
EMAIL_HOST_PASSWORD=  # App password from Gmail (NOT regular password)
DEFAULT_FROM_EMAIL=  # Email shown as "From" in sent emails

# OTP Destination Email
SUPER_ADMIN_OTP_EMAIL=   # Where OTP codes will be sent

# ==========================================
# INSTRUCTIONS:
# 1. Go to https://myaccount.google.com
# 2. Login to  YOUR_GOOGLE_ACCOUNT
# 3. Security  2-Step Verification  App passwords
# 4. Generate app password for "Mail"
# 5. Replace "your-gmail-app-password" above
# ==========================================
